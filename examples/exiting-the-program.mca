#!/usr/local/bin/mca

const math = import('math')

n = 0

while {
    n = n + 1

    V = math.cos(math.rad(n))

    if V >= 1 {
        println(n, V)

        exit(42) # the exit code should be 42 (echo $? (linux))
    }
}

# vim: set syntax=r :
