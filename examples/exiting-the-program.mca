#!/usr/local/bin/mca -i

n = 0

while {
    n = n + 1

    V = cos(rad(n))

    if V >= 1 {
        println(n, V)

        exit(42) # the exit code should be 42 (echo $? (linux))
    }
}

# vim: set syntax=r :
