#!/usr/local/bin/mca

target = 10.0;

a = 0.0;
b = 1.0;
counter = 1.0;

final_result = while counter < target {
    temp = a + b;
    a = b;
    b = temp;
    
    counter = counter + 1.0;
    
    b 
};

println(final_result)

exit(if (final_result == 55.0) 0 else 1)

# vim: set syntax=r :
