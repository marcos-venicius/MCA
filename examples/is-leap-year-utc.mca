year = year(0)

is_leap_year = (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0)

println(is_leap_year)

# we don't have 'not' operator yet
if is_leap_year {
} else {
  exit(1)
}

# vim: set syntax=python :
