#!/usr/local/bin/mca

year = year(0)

is_leap_year = (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0)

println(year, if is_leap_year 'is a lap year' else ' is not a lap year')

if !is_leap_year  exit(1)

# vim: set syntax=r :
