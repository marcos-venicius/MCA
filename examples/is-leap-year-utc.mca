#!/usr/local/bin/mca

current_year = year(0)

is_leap_year = (current_year % 4 == 0 and current_year % 100 != 0) or (current_year % 400 == 0)

println(current_year, if is_leap_year 'is a lap year' else 'is not a lap year')

if !is_leap_year  exit(1)

# vim: set syntax=r :
