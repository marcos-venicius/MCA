#!/usr/local/bin/mca -i

# it should not work at this moment
# we do not have this feature yet

def f(x, y) = x^2 + y^2;

# get the distance between two coordinates
def d(x1, y1, x2, y2) = sqrt((x1 - x2) ^ 2 + (y1 - y2) ^ 2);

# vim: set syntax=python :
