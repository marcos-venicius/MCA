#!/usr/local/bin/mca

# will work soon

a = 0;
b = 1;
n = 0; # counter
M = 15;

LAST_FIB_VALUE = while n < M {
  t = a; # this variable is global
  a = b;
  b = t + b;
  n += 1

  println(a);
};

println();
println();
print('iterations: ', n, '. last fib value: ', LAST_FIB_VALUE, '\n')

# vim: set syntax=r :
