# will work soon

a = 0;
b = 1;
n = 0; # counter
M = 15;

LAST_FIB_VALUE = while n < M {
  t = a; # this variable is global
  a = b;
  b = t + b;
  n = n + 1;

  print(a);
};

print();
print();
print(t, n, LAST_FIB_VALUE)

# vim: set syntax=python :
